{% if package.bundles is defined and package.bundles|length > 0 %}
<span style="display:inline-flex; align-items:center; padding:2px 8px; border-radius:5px; font-size:9px; font-weight:800; text-transform:uppercase; letter-spacing:.06em; background:var(--vv-accent-ghost); border:1px solid color-mix(in srgb,var(--vv-accent),transparent 65%); color:var(--vv-accent);">Bundle</span>
{% endif %}